# Backend API base URL. Do not include a trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Stripe publishable key used by the browser checkout flow.
# Use a test key locally and a live key only in production.
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_your_publishable_key
